[project]
name = "ha-shielddns"
version = "1.2.1-dev0"
description = "Home Assistant integration for ShieldDNS."
authors = [
    {name = "FaserF", email = "github@fabiseitz.de"}
]
readme = "README.md"
requires-python = "==3.14.7"

[tool.ruff]
target-version = "py314"
line-length = 88

[tool.ruff.lint]
select = [
    "E",  # pycodestyle errors
    "W",  # pycodestyle warnings
    "F",  # pyflakes
    "I",  # isort
    "C",  # flake8-comprehensions
    "B",  # flake8-bugbear
    "UP", # pyupgrade
]
ignore = [
    "E501",  # line too long, handled by black
    "C901",  # too complex
]

[tool.mypy]
python_version = "3.14"
mypy_path = "."
explicit_package_bases = true
namespace_packages = true
show_error_codes = true
follow_imports = "silent"
ignore_missing_imports = true
strict_equality = true
warn_incomplete_stub = true
warn_redundant_casts = true
warn_unused_configs = true
warn_unused_ignores = true
check_untyped_defs = true
disallow_incomplete_defs = true
disallow_subclassing_any = false
disallow_untyped_calls = false
disallow_untyped_decorators = false
disallow_untyped_defs = true
disable_error_code = ["misc", "call-arg"]

[[tool.mypy.overrides]]
module = [
    "custom_components.shielddns.*",
]
disallow_subclassing_any = false
disallow_untyped_decorators = false



[tool.pytest.ini_options]
testpaths = [
    "tests",
]
norecursedirs = [
    ".git",
    "testing_config",
]
asyncio_mode = "auto"
pythonpath = ["."]
filterwarnings = [
    "ignore:Inheritance class HomeAssistantApplication from web.Application is discouraged:DeprecationWarning",
    "ignore:unclosed <aiohttp.client.ClientSession:ResourceWarning",
    "ignore:Unclosed client session:ResourceWarning",
]
